<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white">

    <!-- Nội dung màn hình chính -->
    <ScrollView
            android:id="@+id/contentScroll"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:fillViewport="true"
            android:padding="24dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toTopOf="@id/bottomNav"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

        <!-- Khối nội dung, cao = match_parent + gravity center => nằm giữa vùng còn lại -->
        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:gravity="center_horizontal|center_vertical">

            <!-- Hình minh hoạ -->
            <ImageView
                    android:id="@+id/imgDriver"
                    android:layout_width="160dp"
                    android:layout_height="160dp"
                    android:src="@drawable/logo"
                    android:contentDescription="Logo SleepAlert" />

            <!-- Tiêu đề -->
            <TextView
                    android:id="@+id/tvTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="16dp"
                    android:text="Xin chào!"
                    android:textSize="26sp"
                    android:textStyle="bold"
                    android:textColor="#212121" />

            <!-- Sub title -->
            <TextView
                    android:id="@+id/tvSubTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="Chào mừng bạn đến với AI Cảnh Báo Buồn Ngủ"
                    android:textAlignment="center"
                    android:textSize="14sp"
                    android:textColor="#757575" />

            <!-- Card giới thiệu tính năng -->
            <com.google.android.material.card.MaterialCardView
                    android:id="@+id/cardFeatures"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="24dp"
                    app:cardCornerRadius="16dp"
                    app:cardElevation="4dp">

                <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:padding="16dp">

                    <TextView
                            android:id="@+id/tvFeatureTitle"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="🚗 Tính năng"
                            android:textStyle="bold"
                            android:textSize="18sp"
                            android:textColor="#212121" />

                    <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="8dp"
                            android:text="✔ Nhận diện trạng thái buồn ngủ bằng AI"
                            android:textSize="14sp"
                            android:textColor="#424242" />

                    <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="4dp"
                            android:text="✔ Cảnh báo kịp thời khi phát hiện nguy hiểm"
                            android:textSize="14sp"
                            android:textColor="#424242" />

                    <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginTop="4dp"
                            android:text="✔ Theo dõi lịch sử lái xe an toàn"
                            android:textSize="14sp"
                            android:textColor="#424242" />

                </LinearLayout>
            </com.google.android.material.card.MaterialCardView>

            <!-- Nút bắt đầu giám sát -->
            <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnStartMonitoring"
                    android:layout_width="match_parent"
                    android:layout_height="56dp"
                    android:layout_marginTop="24dp"
                    android:text="Bắt đầu giám sát"
                    android:textAllCaps="false"
                    android:textSize="16sp"
                    app:icon="@android:drawable/ic_media_play"
                    app:iconGravity="start"
                    app:iconPadding="12dp"
                    app:cornerRadius="12dp" />

        </LinearLayout>
    </ScrollView>

    <!-- Thanh bottom nav -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNav"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            app:menu="@menu/menu_bottom_nav"
            app:labelVisibilityMode="labeled"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
